- Extract a shared recursive container sanitizer for `_to_bt_safe` model outputs (especially non-Pydantic `model_dump`/`dict` adapters) so nested `dict`/`list` fields can avoid `bt_dumps()`/`bt_loads()` roundtrips without regressing the already-optimized `bt_safe_deep_copy()` hot path.
- Explore a two-mode exact-container walker for `bt_safe_deep_copy` that stays in a no-cycle-tracking fast path until it encounters the first recursively nested container, then upgrades to full visited-set tracking only when needed; the simpler delayed-membership experiments were mixed, but a cleaner split implementation may still beat the current always-probe path.
