# GENERATED by sdk-actions `bin/workflow` — update with: bin/workflow update <this file>
# sdk-actions: {"template":"release/py/turnkey","ref":"71731d4a726df3e3bf42e50c2da4a75842b038dd","version":"1.0.0","params":{"dry_run_environment":"publish-dry-run","emoji":":python:","package_name":"braintrust","publish_environment":"publish","python_version":".tool-versions","slack_channel_var":"SLACK_SDK_RELEASE_CHANNEL","slack_token_secret":"SLACK_BOT_TOKEN","version_file":"py/src/braintrust/version.py","workflow_name":"Publish Python SDK","working_directory":"py"}}

name: Publish Python SDK

on:
  pull_request:
    types: [closed]
    branches: [main]
  push:
    tags:
      - "py-sdk-v*"
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref to publish (branch, tag, or commit SHA)"
        required: true
        type: string
        default: "main"
      release_type:
        description: "Release type to publish to PyPI"
        required: true
        type: choice
        options:
          - stable
          - prerelease
          - auto
        default: stable
      version:
        description: "Version to publish for prereleases (e.g. 0.22.0rc1). Stable releases read version.py."
        required: false
        type: string
      dry_run:
        description: "Build and validate without publishing or creating a GitHub Release"
        required: true
        type: boolean
        default: false

jobs:
  resolve:
    if: github.event_name != 'pull_request' || (github.event.pull_request.merged == true && startsWith(github.event.pull_request.head.ref, 'release/py-sdk-v'))
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions:
      contents: read
    outputs:
      commit_sha: ${{ steps.resolve.outputs.commit_sha }}
      dry_run: ${{ steps.resolve.outputs.dry_run }}
      release_type: ${{ steps.resolve.outputs.release_type }}
      version: ${{ steps.resolve.outputs.version }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.merge_commit_sha || (github.event_name == 'workflow_dispatch' && github.event.inputs.ref || github.ref) }}
          fetch-depth: 0
      - name: Set up mise
        uses: jdx/mise-action@7e36c90d9ab29c415a2384db3006f3ec8a8cc654 # v4.2.4
        with:
          cache: true
          experimental: true
      - name: Resolve release inputs
        id: resolve
        env:
          INPUT_RELEASE_TYPE: ${{ github.event_name == 'workflow_dispatch' && inputs.release_type || 'auto' }}
          INPUT_VERSION: ${{ github.event_name == 'workflow_dispatch' && inputs.version || '' }}
          DRY_RUN: ${{ github.event_name == 'workflow_dispatch' && inputs.dry_run || 'false' }}
        run: |
          VERSION="$INPUT_VERSION"
          if [[ -z "$VERSION" ]]; then
            VERSION=$(mise exec -- python py/scripts/validate-release.py auto --print-version)
          fi

          RELEASE_TYPE="$INPUT_RELEASE_TYPE"
          if [[ "$RELEASE_TYPE" == "auto" ]]; then
            if [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+(a|b|rc)[0-9]+$ ]]; then
              RELEASE_TYPE=prerelease
            else
              RELEASE_TYPE=stable
            fi
          fi

          mise exec -- python py/scripts/validate-release.py "$RELEASE_TYPE" \
            --version "$VERSION" --validate-version-only

          if [[ "${{ github.event_name }}" == "push" && "${{ github.ref_name }}" != "py-sdk-v$VERSION" ]]; then
            echo "Tag ${{ github.ref_name }} does not match version $VERSION" >&2
            exit 1
          fi

          {
            echo "commit_sha=$(git rev-parse HEAD)"
            echo "dry_run=$DRY_RUN"
            echo "release_type=$RELEASE_TYPE"
            echo "version=$VERSION"
          } >> "$GITHUB_OUTPUT"

  configure:
    needs: resolve
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions:
      contents: write
    outputs:
      version: ${{ steps.configure.outputs.version }}
      release_tag: ${{ steps.configure.outputs.release_tag }}
      prev_release: ${{ steps.configure.outputs.prev_release }}
      branch: ${{ steps.configure.outputs.branch }}
      on_release_branch: ${{ steps.configure.outputs.on_release_branch }}
      commit_message: ${{ steps.configure.outputs.commit_message }}
      github_release: ${{ steps.configure.outputs.github_release }}
      already_published: ${{ steps.configure.outputs.already_published }}
      notes: ${{ steps.configure.outputs.notes }}
      pr_list: ${{ steps.configure.outputs.pr_list }}
      package: ${{ steps.configure.outputs.package }}
    steps:
      - name: Configure release
        id: configure
        uses: braintrustdata/sdk-actions/actions/release/lang/py/configure@71731d4a726df3e3bf42e50c2da4a75842b038dd
        with:
          sha: ${{ needs.resolve.outputs.commit_sha }}
          version: ${{ needs.resolve.outputs.version }}
          working_directory: py
          tag_format: py-sdk-v{version}
          release_type: ${{ needs.resolve.outputs.release_type }}
          release_branch: main
          pypi_package_name: braintrust
          package_label: Braintrust Python SDK
          emoji: ':python:'

  validate:
    needs: [resolve, configure]
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    permissions:
      contents: read
    env:
      BRAINTRUST_RELEASE_CHANNEL: ${{ needs.resolve.outputs.release_type }}
      BRAINTRUST_VERSION_OVERRIDE: ${{ needs.resolve.outputs.version }}
    steps:
      - name: Validate release
        uses: braintrustdata/sdk-actions/actions/release/lang/py/validate@71731d4a726df3e3bf42e50c2da4a75842b038dd
        with:
          sha: ${{ needs.resolve.outputs.commit_sha }}
          dry_run: ${{ needs.resolve.outputs.dry_run }}
          working_directory: py
          python_version: .tool-versions
          release_tag: ${{ needs.configure.outputs.release_tag }}
          on_release_branch: ${{ needs.configure.outputs.on_release_branch }}
          notes: ${{ needs.configure.outputs.notes }}
          already_published: ${{ needs.configure.outputs.already_published }}
          build_command: make install-dev verify-build
          enforce_release_branch: 'true'
          check_tag_unused: ${{ github.event_name != 'push' }}
          check_version_unpublished: ${{ github.event_name != 'push' }}

  request-approval:
    needs: [resolve, configure, validate]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions: {}
    steps:
      - name: Request release approval
        uses: braintrustdata/sdk-actions/actions/release/request-approval@71731d4a726df3e3bf42e50c2da4a75842b038dd
        with:
          packages: '{"packages":[${{ needs.configure.outputs.package }}]}'
          sha: ${{ needs.resolve.outputs.commit_sha }}
          branch: ${{ needs.configure.outputs.branch }}
          on_release_branch: ${{ needs.configure.outputs.on_release_branch }}
          commit_message: ${{ needs.configure.outputs.commit_message }}
          dry_run: ${{ needs.resolve.outputs.dry_run }}
          slack_token: ${{ secrets.SLACK_BOT_TOKEN }}
          slack_channel: ${{ vars.SLACK_SDK_RELEASE_CHANNEL }}

  build-and-ship:
    needs: [resolve, configure, validate, request-approval]
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    environment: ${{ needs.resolve.outputs.dry_run == 'true' && 'publish-dry-run' || 'publish' }}
    permissions:
      contents: write
      id-token: write
      attestations: write
    env:
      BRAINTRUST_RELEASE_CHANNEL: ${{ needs.resolve.outputs.release_type }}
      BRAINTRUST_VERSION_OVERRIDE: ${{ needs.resolve.outputs.version }}
    steps:
      - name: Build and ship
        uses: braintrustdata/sdk-actions/actions/release/lang/py/build-and-ship@71731d4a726df3e3bf42e50c2da4a75842b038dd
        with:
          sha: ${{ needs.resolve.outputs.commit_sha }}
          working_directory: py
          python_version: .tool-versions
          build_command: make install-dev verify-build
          dry_run: ${{ needs.resolve.outputs.dry_run }}
          release_tag: ${{ needs.configure.outputs.release_tag }}
          already_published: ${{ needs.configure.outputs.already_published }}
          github_release: ${{ needs.configure.outputs.github_release }}
          release_title: Python SDK v${{ needs.configure.outputs.version }}
          version: ${{ needs.configure.outputs.version }}
          package_name: braintrust
          label: Braintrust Python SDK
          notes: ${{ needs.configure.outputs.notes }}
          prev_release: ${{ needs.configure.outputs.prev_release }}
          branch: ${{ needs.configure.outputs.branch }}
          on_release_branch: ${{ needs.configure.outputs.on_release_branch }}
          pr_list: ${{ needs.configure.outputs.pr_list }}
          slack_token: ${{ secrets.SLACK_BOT_TOKEN }}
          slack_channel: ${{ vars.SLACK_SDK_RELEASE_CHANNEL }}
          emoji: ':python:'
